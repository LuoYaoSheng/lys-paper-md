# PaperMD 产品模型（PRODUCT_MODEL）

> 版本：v1.0　日期：2026-09-03
> 事实来源：`docs/02_product/PRD.md` §1-§3（其原始出处：旧项目源码、`README.md`、`CLAUDE.md`、`docs/产品说明.md`、`docs/ROADMAP.md`）+ `docs/01_reverse/REVERSE_ANALYSIS.md` ①1.1/1.3/1.4
> 说明：本文件为 PRODUCT_MODEL 标准交付物，内容自上述文档聚合重组，不新增事实。

---

## 1. 产品定位

**PaperMD = 原生 macOS Markdown 语法高亮编辑器**，保留源码可见（`#`、`**` 等标记不隐藏），以「极致输入体验」为最高优先级；本地优先，文件即文档，无私有格式，无云端依赖。（来源：`docs/02_product/PRD.md` §1）

- 一句话价值主张：**写起来最像在思考的 Markdown 编辑器**（原始出处 `docs/产品说明.md` 1.2，经 PRD §1 转引）。
- 编辑模型：单栏「带样式的源码」（styled source）——语法高亮以属性叠加呈现，标记保持可见；**不是**隐藏标记的 WYSIWYG（`CLAUDE.md` "Edit model"）。
- 核心理念：「输入零卡顿、光标行为 100% 可预测」，为不可妥协的 P0（`CLAUDE.md` "Core Philosophy"）。

### 产品四铁律（P0，全员共识约束）

1. 光标位置必须符合用户预期，永不因渲染跳动。
2. 自动格式化绝不移动光标。
3. 中文输入法（IME）组合期间不进行布局重建或结构转换。
4. 每个结构变更（含图片插入）必须可撤销。

（来源：`README.md` 开发理念 / `CLAUDE.md`，经 PRD §1 转引）

### 产品边界（v1 明确不做）

云同步、多人协作、插件、AI 写作、账号体系、隐藏标记 WYSIWYG。（来源：`CLAUDE.md` "Out of scope"、`docs/产品说明.md` 第 9 节、`docs/ROADMAP.md` v1.0 不做清单）

## 2. 用户角色（画像）

| 画像 | 特征 | 核心诉求 | 对应功能（F 编号） |
|------|------|----------|--------------------|
| 开发者 | 写 README/技术文档/设计说明，英文中文混输，重度快捷键用户 | 快、可预测、纯文本保真 | F011-F013、F032-F039、F040 |
| 内容创作者 | 教程/博客/公众号长文，粘贴图片频繁 | 图片自动落盘、不破坏 .md、统计字数 | F014-F018、F031 |
| 长文写作者 | 方案/手册/连续写作 1 小时+ | 大纲导航、专注模式、自动保存 | F028-F031、F008 |
| 中文写作用户 | 输入法组合频繁 | IME 稳定、不丢字不跳标 | F047 |

（来源：`docs/02_product/PRD.md` §2，原始出处 `docs/产品说明.md` 第 2 节 + REVERSE_ANALYSIS.md ①1.3）

非目标用户（v1 明确不满足）：实时协作用户、强笔记/知识图谱用户、AI 写作依赖用户、需要云同步的用户。（同上）

> 特别说明：中文（IME）输入体验被列为一等公民（`docs/产品说明.md` 5.3.3、`README.md` 开发理念，经 REVERSE_ANALYSIS.md ①1.3 转引）。

## 3. 使用场景（七组）

| # | 场景 | 行为链 | 涉及功能 |
|---|------|--------|----------|
| 1 | 快速开写 | 双击打开 App → 1 秒内进入编辑态，光标已在文档中（欢迎文本预填） | F001/F053 |
| 2 | 连续写作 1 小时 | 无输入延迟、无 UI 抖动、无误触焦点丢失；3 秒自动保存兜底 | F008/F047/F048 |
| 3 | 插入结构内容 | `#` 出标题、```` ``` ```` 出代码块；Enter 自动续列表；Tab 缩进 | F036/F038/F043-F046 |
| 4 | 插入图片 | 截图后直接 ⌘V，图片自动写入 `{文档名}.assets/`，正文出现 `![](相对路径)` 源文本 | F014-F018 |
| 5 | 导出交付 | ⌃⌘E 导出带主题 CSS 的 HTML；⌃⌘P 经打印面板存 PDF | F009/F010 |
| 6 | 长文导航 | 侧栏大纲点击跳转标题行；⌘F 查找替换（含正则） | F019-F023/F030 |
| 7 | 深度专注 | ⌃⌘F 进入专注模式，侧栏/状态栏/工具栏全部隐去 | F029 |

（来源：`docs/02_product/PRD.md` §3）

## 4. 核心价值（八大特性）

1. 原生 macOS 应用：AppKit 构建，性能与体验原生。
2. 语法高亮编辑：单栏编辑、实时高亮、非隐藏标记。
3. 智能编辑：自动列表续行、Tab 缩进、智能列表项终止。
4. 大纲视图：标题自动生成目录导航。
5. 专注模式：隐藏干扰（侧栏+状态栏+工具栏）。
6. 全键盘支持：所有操作有快捷键。
7. 纯文本粘贴：外部粘贴自动去格式。
8. 四条开发铁律（见 §1）。

（来源：`README.md` 核心特性 + 开发理念，经 REVERSE_ANALYSIS.md ①1.4 转引）

### 优先级轴（决策用）

P0 输入体验 > P1 文件/导出/大纲 > P2 UI 美观。（来源：`docs/产品说明.md` 第 3 节，经 PRD §5 转引）
